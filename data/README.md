# Seed Data (Stage 2 output)

Authored by Agent 2 (Data Engineer) — strict downstream of Stage 1 schemas. Regenerated by:

```bash
python3 tools/seed_builder.py
```

| File | Count | Schema |
|---|---|---|
| `exercises.seed.json` | 60 | `schemas/exercise.schema.json` |
| `muscle-groups.seed.json` | 6 | `schemas/muscle-group.schema.json` |
| `workout-templates.seed.json` | 36 (4 splits × 3 body types × 3 goals) | `schemas/workout-template.schema.json` |
| `seed.meta.json` | 1 | — |
| `enums.json` | — | Stage 1 canonical enums (do not edit without re-running Agent 1) |

The builder script in `tools/seed_builder.py` also runs:

1. Draft-07 schema validation against every output object.
2. Cross-reference checks: every `swap_candidate` and every `slot.candidate_pool` ID resolves to an existing exercise; every `primary_target_zone` belongs to its declared `primary_muscle_group`.
3. Equipment coverage check: `HOME_INTERMEDIATE`, `HOME_ADVANCED`, `COMMERCIAL_GYM` each meet the per-muscle-group minimums from `docs/architecture/03-equipment-tiers.md` §4.

If any check fails the script exits non-zero — Agent 5 will wire this into CI.
